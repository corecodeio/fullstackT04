# Backend

## Installed packages

```
$ npm install --save express
$ npm install --save-dev nodemon
$ npm install --save dotenv
$ npm install cors
```
## .env
```
#SERVER CONFIGURATIONS
SERVER_PORT=8500
```